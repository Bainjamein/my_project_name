{% extends 'base.html.twig' %}
    {% block body %}
        <h1>Mes posts</h1>

        {% for post in posts %}
            <li>{{ post.getTitle }}</li>
        {% endfor %}
    
    
    
{% endblock %}